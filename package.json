{
  "name": "@skorify/shared",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepare": "pnpm build",
    "test": "jest",
    "test:watch": "jest --watch",
    "build": "npx tsc -p tsconfig.json",
    "build:dev": "npx tsc -p tsconfig.json && pnpm run copy-package && pnpm run copy-all",
    "copy-package": "copyfiles  package.json dist/",
    "copy-all": "copyfiles -u 1 dist/**/* ../server/libs/shared",
    "seed": "node src/fixtures/seed.js",
    "seed:clean": "node src/fixtures/seed.js --clean"
  },
  "dependencies": {
    "@aws-sdk/client-eventbridge": "^3.1047.0",
    "@aws-sdk/client-s3": "^3.1047.0",
    "@aws-sdk/s3-request-presigner": "^3.1047.0",
    "@aws-sdk/client-sns": "^3.1047.0",
    "@skorify/domain": "github:aws-ug-manizales/Skorify_Domain#main"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.2",
    "copyfiles": "^2.4.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "5.7.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.22.0"
}
